{
  "name": "vos-mcp-server",
  "version": "1.0.0",
  "description": "MCP server for interacting with VOS running in QEMU",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "vos-mcp-server": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "mcp",
    "vos",
    "qemu",
    "os-development"
  ],
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.6.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "ts-node": "^10.9.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
